External reference PDFs are intentionally excluded from this repository.

Only source code and build manifests required to rebuild Sentinel stay versioned here. Reference documentation should be tracked in a separate document repository with its own provenance and access controls.
