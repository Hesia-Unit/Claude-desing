`hesia-policy-pqc` is intentionally not stored as a prebuilt binary in this repository anymore.

Use the checked-in source (`hesia-policy-pqc.cpp`) and wrapper script (`hesia-policy-pqc.sh`) to rebuild the tool from source on the target toolchain.
